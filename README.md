# b64

> [!TIP]
> Why aren't you using [`base64`](https://linux.die.net/man/1/base64) instead?

Encode and decode base-64 strings.

For documentation, see [./example.sh](./example.sh) and `go run . -help`.
